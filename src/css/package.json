{
  "name": "laliga-app",
  "version": "1.0.0",
  "description": "La Liga 2025-26 Standings and Statistics",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "vercel-build": "npm run build",
    "deploy:vercel": "vercel --prod",
    "deploy:vercel:force": "vercel --prod --force",
    "update:data": "node scripts/update-data.js",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "devDependencies": {
    "@vercel/node": "^2.15.10",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "axios": "^1.6.5"
  }
}